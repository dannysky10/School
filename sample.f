!    First Program in fortran

      program sample
      real r, area
      write (*,*) 'radius r: '
      read (*,*) r
      area = 3.14159*r*r
      write (*,*) 'Area = ', area

      end program sample